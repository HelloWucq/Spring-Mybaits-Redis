spring:
    datasource:
        jdbc.url: jdbc:mysql://localhost/test
        jdbc.username: root
        jdbc.password: wuchenqi
        jdbc.diver-class-name: com.mysql.jdbc.Driver

mybatis:
    type-aliases-package: com.wucq.webdemo.dao.domain
    mapper-locations:
        - mappers/ProductMapper.xml

redis:
    database: 3
    host: localhost
    port: 6379
    password:
    timeout: 0
    pool:
        max-active:8
        max-idle:8
        min-idle:0
        max-wait:-1

server:
    port: 8888

logging:
    level:
        com.wucq.webdemo: DEBUG

